class Solution(object):
    def sortedSquares(self, nums):
      #1 way => Time: O(n + nlogn), space: O(1)
      return sorted((map(lambda x: pow(x, 2), nums)) 
      
      
 #Link to problem: https://leetcode.com/problems/squares-of-a-sorted-array/description/
